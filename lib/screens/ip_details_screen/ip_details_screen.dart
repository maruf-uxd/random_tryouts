import 'package:flutter/material.dart';
import 'package:random_tryouts/screens/ip_details_screen/components/body.dart';

class IPDetailsScreen extends StatelessWidget {
  const IPDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
